export default defineAppConfig({
  ui: {
    // Editorial mono palette: Nuxt UI primary is just neutral so switches,
    // focus rings, etc. follow the same ink tone as the rest of the app.
    colors: {
      primary: 'neutral',
      neutral: 'zinc'
    }
  }
})